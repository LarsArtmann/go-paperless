# Features

> Honest inventory of what this project does, by status. Generated by studying
> the actual code, not the marketing claims. Updated as features ship, change,
> or break. Citations anchor to symbols and test names — line numbers rot.

## Status legend

| Status                    | Meaning                                                      |
| ------------------------- | ------------------------------------------------------------ |
| 🟢 `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| 🟡 `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| 🔴 `BROKEN`               | Present in code but not working / disabled / failing.        |
| ⚪ `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

> A feature earns `FULLY_FUNCTIONAL` only when you can point to the code that
> delivers it AND confirm it works. If you're unsure, it's `PARTIALLY_FUNCTIONAL`
> at best, never round up.

## Client construction and configuration

| Feature                             | Status                | Notes                                                                                                                                        |
| ----------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Client construction with validation | 🟢 `FULLY_FUNCTIONAL` | `New` rejects empty URL/token and non-HTTP schemes, returns `ErrInvalidConfig`; `TestNewRejectsNegativeRetryMaxAttempts`, `ExampleNew_invalidConfig` |
| Construction options                | 🟢 `FULLY_FUNCTIONAL` | `WithHTTPClient` and `WithTimeout` (last option wins); `TestWithHTTPClientRoutesRequestsThroughSuppliedClient`, `TestWithTimeoutBoundsSlowResponses`  |

## Document ingestion

| Feature                   | Status                | Notes                                                                                                                                                    |
| ------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Multipart document upload | 🟢 `FULLY_FUNCTIONAL` | `Upload` posts to the consumption queue and returns the task UUID; wire format asserted in `TestUploadSendsMultipartWithDocumentField`                    |
| Upload metadata fields    | 🟢 `FULLY_FUNCTIONAL` | title, created, correspondent, tags, document_type, custom_fields via `writeUploadMetadata`; `TestUploadSendsDocumentTypeAndCustomFields`, `TestUploadSendsCorrespondentField` |

## Async consumption tracking

| Feature                             | Status                | Notes                                                                                                                                                                                                                        |
| ----------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Task polling by ID                  | 🟢 `FULLY_FUNCTIONAL` | `GetTask` filters `/api/tasks/` by task_id, `found=false` for empty results; `TestGetTaskNotFound`, `TestGetTaskToleratesBareArray`                                                                                          |
| Outcome classification              | 🟢 `FULLY_FUNCTIONAL` | `classifyTask` prefers `result_data.document_id`, falls back to `duplicate_of`, then `related_document_ids`; consumed/duplicate/failure/pending/malformed table: `TestGetTaskClassifiesConsumedDocument` … `TestGetTaskRejectsMalformedJSON` |
| Duplicate-refusal detection         | 🟢 `FULLY_FUNCTIONAL` | `TaskOutcome.DuplicateRefused`/`DuplicateInTrash` separate checksum-dedup refusals from real failures; `TestGetTaskClassifiesDuplicateRefusal`                                                                               |
| Terminal-state model                | 🟢 `FULLY_FUNCTIONAL` | `TaskStatus.Terminal` treats unknown future statuses as non-terminal, the safe default for retry decisions                                                                                                                   |
| `WaitForTask` (poll until terminal) | 🟢 `FULLY_FUNCTIONAL` | Immediate first poll, not-found/transient tolerance, duplicate refusal is not an error, ctx deadline → `paperless.task_poll_abandoned`; seven dedicated tests `TestWaitForTaskPendingThenSuccess` … `TestWaitForTaskWithRetryRecoversFromTransientPollFailure` |
| `TaskOutcome.Duplicate()` accessor  | 🟢 `FULLY_FUNCTIONAL` | Returns `(documentID, inTrash, refused)`, zero values for non-refusals; `TestTaskOutcomeDuplicateReportsRefusalDetails`                                                                                                       |

## Metadata lookups (idempotent)

| Feature                                       | Status                | Notes                                                                                                                                                                       |
| --------------------------------------------- | --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `EnsureTag` (never joins the classifier)      | 🟢 `FULLY_FUNCTIONAL` | Creates with matching_algorithm "none" and self-heals legacy auto tags via PATCH; four dedicated tests `TestEnsureTagFindsExisting` … `TestEnsureTagCreatesWhenMissing`      |
| `EnsureCorrespondent` (keeps "auto")          | 🟢 `FULLY_FUNCTIONAL` | Auto matching kept deliberately for correspondents; `TestEnsureCorrespondentFindsExisting`, `TestEnsureCorrespondentCreatesWhenMissing`                                      |
| `EnsureDocumentType` (find or create)         | 🟢 `FULLY_FUNCTIONAL` | An existing type keeps its configured algorithm, no self-heal; `TestEnsureDocumentTypeFindsAndCreates`                                                                       |
| `EnsureCustomField` / `FindCustomField`       | 🟢 `FULLY_FUNCTIONAL` | Read-only lookup plus create-as-string when missing; `TestEnsureAndFindCustomField`                                                                                          |
| `EnsureStoragePath` (keeps existing template) | 🟢 `FULLY_FUNCTIONAL` | POSTs only `{name, path}` (slug is server-generated); an existing path is returned untouched; `TestEnsureStoragePathCreatesWhenMissing` … `TestEnsureStoragePathRejectsEmptyArgs` |
| `FindStoragePath` / `ListStoragePaths`        | 🟢 `FULLY_FUNCTIONAL` | Case-insensitive name lookup plus bounded (≤100 pages) full listing via `fetchAllPages`; `TestFindStoragePathReturnsFoundFlag`, `TestListStoragePathsPaginatesAndMaps`       |

## Name resolution

| Feature            | Status                | Notes                                                                                        |
| ------------------ | --------------------- | ---------------------------------------------------------------------------------------------- |
| ID to display name | 🟢 `FULLY_FUNCTIONAL` | `GetCorrespondentName` and `GetDocumentTypeName` via the detail endpoints                     |

## Document management

| Feature               | Status                | Notes                                                                                                                                                                                                                                                |
| --------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Checksum listing      | 🟢 `FULLY_FUNCTIONAL` | `ListDocumentChecksums` paginates via the shared `fetchAllPages` helper (100/page, ≤100 pages) and reads both flat and 3.x `versions[]` checksums; `TestListDocumentChecksumsPaginates`, `TestListDocumentChecksumsCapStopsAtMaxPages`, `TestListDocumentChecksumsConcurrentCalls` |
| Full metadata listing | 🟢 `FULLY_FUNCTIONAL` | `ListDocumentMetas` returns `DocumentMeta` incl. custom fields and effective checksum; `TestListDocumentMetasReturnsFields` (custom-fields round-trip)                                                                                              |
| Metadata update       | 🟢 `FULLY_FUNCTIONAL` | `UpdateDocument` PATCHes only set fields and rejects empty requests before the round trip; `TestUpdateDocumentSendsPatchBody`, `TestUpdateDocumentRejectsEmptyRequest`                                                                                |
| Delete document       | 🟢 `FULLY_FUNCTIONAL` | `DeleteDocument`; `TestDeleteDocumentSendsDelete`                                                                                                                                                                                                     |
| Download document     | 🟢 `FULLY_FUNCTIONAL` | `DownloadDocument` fetches the original file bytes; happy path + 404 error-wrap tests                                                                                                                                                                  |

## Server compatibility

| Feature            | Status                | Notes                                                                                                                                                      |
| ------------------ | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Ping               | 🟢 `FULLY_FUNCTIONAL` | Targets the documents list, not the HTML-only API root that 406s JSON; contract test `TestPingAgainstHtmlOnlyApiRoot`                                       |
| Capability probing | 🟢 `FULLY_FUNCTIONAL` | `ProbeCapabilities` samples one page and reports checksum shape plus negotiated API version; `TestProbeCapabilitiesDetectsChecksumShapes`, `TestNegotiatedAPIVersionReadsContentType` |

## Error handling and retries

| Feature                     | Status                | Notes                                                                                                                                                                                                                                                                                                                            |
| --------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Error-family classification | 🟢 `FULLY_FUNCTIONAL` | 401/403 → Rejection, 429/5xx → Transient, other 4xx → Rejection with body snippet (`classifyStatus`); asserted across upload/ping/list tests (`TestUploadClassifiesAuthFailure`, `TestUploadClassifiesServerErrorAsTransient`)                                   |
| `Retry-After` hints         | 🟢 `FULLY_FUNCTIONAL` | `RetryAfterError` wraps 429/503 with a parsed delay, seconds or HTTP-date; delay-seconds too large for `time.Duration` are unparseable (fuzz-found overflow, `parseRetryAfter`); `TestParseRetryAfter`, `TestUpload429CarriesRetryAfterHint`, `TestUpload503CarriesRetryAfterHint` |
| Opt-in automatic retries    | 🟢 `FULLY_FUNCTIONAL` | `WithRetry(RetryPolicy)` retries transient failures only (network/429/5xx), rejections fail fast, request bodies replay byte-for-byte per attempt, server `Retry-After` overrides backoff, negative `MaxAttempts` rejected via `ErrInvalidConfig`; eight tests `TestWithRetryDefaultOffMakesSingleAttempt` … `TestRetryPolicyZeroValueFillsDefaults` |

## Observability hooks

| Feature                | Status                | Notes                                                                                                                                                                                                                                                                                                                   |
| ---------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Request/response hooks | 🟢 `FULLY_FUNCTIONAL` | `WithRequestHook`/`WithResponseHook` receive value snapshots with cloned headers — they include the Authorization token / Set-Cookie, redact before logging; non-2xx bodies capped at 512 bytes; `TestRequestHookObservesWithoutMutating`, `TestResponseHookSees2xxBodyAndCappedErrorBody`, `TestProbeCapabilitiesFiresHooks` |

## Document notes, share links, and saved views

| Feature                          | Status                | Notes                                                                                                                                                                                                                               |
| -------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Document notes (list/add/delete) | 🟢 `FULLY_FUNCTIONAL` | Bare-array endpoint; every mutation answers with the updated list, delete via `?id=` (`ListDocumentNotes`/`AddDocumentNote`/`DeleteDocumentNote`); wire-shape tests `TestListDocumentNotesReturnsBareArray` etc.                        |
| Share links (list/create/delete) | 🟢 `FULLY_FUNCTIONAL` | Server generates the slug (never sent); string `file_version` (archive/original, zero value omitted); optional expiration (`ListShareLinks`/`CreateShareLink`/`DeleteShareLink`); pagination + wire tests `TestListShareLinksPaginates`, `TestCreateShareLinkSendsDocumentAndFileVersion`, `TestListShareLinksCapStopsAtMaxPages` |
| Saved views (list/create/delete) | 🟢 `FULLY_FUNCTIONAL` | Stable-core fields incl. `filter_rules` (rule_type + value); newer/older server UI fields are ignored (`ListSavedViews`/`CreateSavedView`/`DeleteSavedView`); `TestListSavedViewsMapsFilterRules`, `TestCreateSavedViewSendsStableFields`, `TestListSavedViewsCapStopsAtMaxPages` |

## Planned

None. Ideas for future coverage live in [ROADMAP.md](ROADMAP.md).
