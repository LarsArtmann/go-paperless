# TODO List

> Short-term, actionable, bounded work items, verified against the actual code.
> For long-term vision and unrefined ideas, use ROADMAP.md.
> Items are ranked by impact. Status is verified, not assumed. Completed rows
> are deleted (they live in `CHANGELOG.md`), never retained.

## Status legend

| Status           | Meaning                                                     |
| ---------------- | ----------------------------------------------------------- |
| 🔴 `TODO`        | Not started. Needs doing.                                   |
| 🟡 `IN_PROGRESS` | Actively being worked on.                                   |
| 🔵 `BLOCKED`     | Cannot proceed, external dependency or decision needed.     |
| 🟢 `DONE`        | Completed. Remove from this list and log in `CHANGELOG.md`. |

## High Impact

| Task                                                            | Status       | Impact | Effort | Evidence                                                                                                                                              |
| --------------------------------------------------------------- | ------------ | ------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Push bank-sync + InboxClean consumer bumps (go 1.27 + v0.3.0)   | 🔵 `BLOCKED` | High   | ~10min | Both bumped and green locally, not pushed — the v0.3.0 rollout exists on one machine until then (`docs/status/…05-24….md` b-4, g-1)                   |
| Decide: retire bank-sync's trimmed fork in favor of this module | 🔵 `BLOCKED` | High   | ~2h    | `AGENTS.md` scope note calls this module a drop-in replacement for the fork; decision + migration belongs to bank-sync (`docs/status/…05-24….md` c-2) |

## Medium Impact

| Task                                                                                                                                         | Status    | Impact | Effort | Evidence                                                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decide the erraudit CI gate: install path, private-runner auth, golangci analyzer, or close with the decision recorded                       | 🔴 `TODO` | Med    | ~30min | erraudit reports zero findings (re-verified post-05-24 session, dev shell); public installability unverified — the binary is local-only (`docs/status/…18-37….md` g-1, `…05-24….md` b-5)        |
| Extend the integration scaffold with the real e2e: upload → `WaitForTask` → checksums reconcile                                              | 🔴 `TODO` | Med    | ~2h    | `integration_test.go` covers ping + the five listings; the upload reconciliation loop — the actual value — is missing (`docs/status/…05-24….md` b-3)                                            |
| Verify tagged releases build clean in-sandbox (`nix build` from worktrees of v0.2.0/v0.3.0)                                                  | 🔴 `TODO` | Med    | ~30min | Settles whether a red lint gate shipped inside a tag: the failed derivation was `go-paperless-0.2.0`, never correlated with the tag commit (`docs/status/…04-13….md` d-3, f-2)                  |
| Audit the 8 non-mechanical lint fixes (dupl ×5, errorlint, wrapcheck, nonamedreturns, modernize-wg) — confirm mechanism, not just green gate | 🔴 `TODO` | Med    | ~1h    | Gate is green but the fix hunks were never read; unverified attribution is a trust bug (`docs/status/…04-13….md` b-2, d-1)                                                                      |
| gosec CI step: assert scanned Files > 0, fail closed                                                                                         | 🔴 `TODO` | Med    | ~15min | `ci.yml` runs gosec strict (no `-no-fail`) but nothing asserts the scanner actually loaded files — the exact v0.1.1 failure class                                                               |
| Coverage recovery: name the uncovered blocks behind 88.6% and close the top three                                                            | 🔴 `TODO` | Med    | ~1h    | 90.1% → 88.6% from ~500 lines of new API code; candidates: retry-policy edges, saved-view create error paths (`docs/status/…05-24….md` b-2, f-8)                                                |
| Cap-parity tests for `ListDocumentMetas` + `ListStoragePaths`                                                                                | 🔴 `TODO` | Med    | ~30min | Only 3 of 5 listings have a `CapStopsAtMaxPages` test — the "all five listings" claim in `docs/status/…05-24….md` a-2 overcovered (verified 2026-09-14)                                         |
| Wire `docs/ERROR_CODES.md` into CI: a test greps the catalog for every `paperless.*` literal in `client.go`                                  | 🔴 `TODO` | Med    | ~30min | Kills catalog doc-drift mechanically (`docs/status/…05-24….md` f-12)                                                                                                                            |
| Fuzz `savedViewPayload`/`shareLinkPayload` decoding                                                                                          | 🔴 `TODO` | Med    | ~30min | The new families have hand-written payloads; only checksums/retry-after are fuzzed today (`docs/status/…05-24….md` f-21)                                                                        |
| SECURITY.md: share-link threat-model paragraph (public unauthenticated URL exposure)                                                         | 🔴 `TODO` | Med    | ~15min | Share links widen the auth surface; the doc predates them (`docs/status/…03-29….md` f-27)                                                                                                       |
| Typed enums for wire-string fields: share-link `FileVersion`/expiry; saved-view `rule_type`                                                  | 🔴 `TODO` | Med    | ~1h    | `file_version` is a string ("archive"/"original") and `filter_rules[].rule_type` is a bare int; typed enums would mirror `matchingAlgorithm` (`docs/status/…04-13….md` f-22, `…03-29….md` f-22) |
| Flake `version` git-derived (`self.rev`/`dirtyRev`) or bumped inside the release commit                                                      | 🔴 `TODO` | Med    | ~45min | The attribute drifted to 0.2.0 while `v0.3.0` shipped — the release flow demonstrably excludes it (`docs/status/…04-13….md` d-3, f-7)                                                           |

## Low Impact

| Task                                                                                                                                                                  | Status    | Impact | Effort | Evidence                                                                                                                                                                                           |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T23: streaming-upload spike note (io.Reader body + Content-Length constraints)                                                                                        | 🔴 `TODO` | Low    | ~1h    | Decision record before anyone assumes the ROADMAP feature is promised (`docs/status/…03-29….md` c-3)                                                                                               |
| Examples batch: `ExampleClient_WaitForTask` + retry example; add `// Output:` to `ExampleNew`/`ExampleNew_withOptions` and drop their `testableexamples` suppressions | 🔴 `TODO` | Low    | ~45min | `example_test.go` has neither example; the two runnable ones carry suppressions instead of Outputs (`docs/status/…05-24….md` b-6, f-7)                                                             |
| Confirm `.crushrc` cleared the gopls/golangci-lint-ls failures after a full Crush restart; settle `--env GOTOOLCHAIN auto` vs `=auto` syntax                          | 🔴 `TODO` | Low    | ~10min | In-session `lsp_restart` provably does not pick up the pin; needs a Crush restart (`docs/status/…05-24….md` c-1, g-3)                                                                              |
| `docs/adr/` index/README + ADR template; cross-link from ROADMAP                                                                                                      | 🔴 `TODO` | Low    | ~20min | Single ADR file, no scaffold/index page (`docs/status/…05-24….md` c-4)                                                                                                                             |
| Make the `t.Cleanup`-vs-`defer server.Close()` call once, record it in AGENTS.md                                                                                      | 🔴 `TODO` | Low    | ~15min | 63 sites use defer; the plan asked for an explicit decision, not a shrug (`docs/status/…05-24….md` b-1, f-11)                                                                                      |
| Flake/CI wiring for the integration scaffold: `go vet -tags integration` gate + opt-in `checks.integration` app                                                       | 🔴 `TODO` | Low    | ~30min | The scaffold can currently rot uncompiled; a live run should be opt-in, not in `nix flake check` (`docs/status/…05-24….md` f-16, f-20, f-46)                                                       |
| Post-release ritual as one command (push status, CI green, pkg.go.dev, race)                                                                                          | 🔴 `TODO` | Low    | ~1h    | The repo has been bitten twice (indexing gap, version drift); the ritual should not live in memory (`docs/status/…04-13….md` e-8, f-41)                                                            |
| Verb-table drift test: assert every exported `Find*`/`Get*`/`Ensure*` name appears in the README table                                                                | 🔴 `TODO` | Low    | ~20min | Mechanical README↔code honesty, same idea as the error-catalog test (`docs/status/…05-24….md` f-13)                                                                                                |
| README quick-start snippets for document notes / share links / saved views                                                                                            | 🔴 `TODO` | Low    | ~30min | The three v0.3.0 families have FEATURES rows but no copy-paste-able example (`docs/status/…05-24….md` f-14)                                                                                        |
| Tests tail: `defaultTransport` honors exported constants; `WithHTTPClient(nil)` defensive branch; `UpdateDocument` custom_fields PATCH/empty-`TagIDs` assertion       | 🔴 `TODO` | Low    | ~30min | All three verified missing on 2026-09-14 (no `defaultTransport`/`WithHTTPClient(nil)` test; `TestUpdateDocumentSendsPatchBody` never asserts custom_fields) — the T06–T08 "DONE" claim overcovered |
